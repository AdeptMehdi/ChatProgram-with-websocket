# 🚀 سامانه چت بی‌درنگ پیشرفته (Advanced Real-Time Chat Engine)

یک پلتفرم چت سازمانی و مقیاس‌پذیر با معماری مدرن، طراحی خیره‌کننده **Glassmorphism 2.0** و قابلیت‌های مدیریتی کامل. این پروژه با استفاده از استک تکنولوژی **Astro**، **Node.js** و **Socket.io** بر پایه اصول مهندسی نرم‌افزار توسعه یافته است.

---

## 🏗 معماری سیستم (System Architecture)

### ۱. متدولوژی SOP (Service-Oriented Programming)
این پروژه از توپولوژی **SOP** برای مدیریت پیچیدگی‌ها استفاده می‌کند. برخلاف رویکردهای سنتی، منطق تجاری (Business Logic) از لایه انتقال (Transport Layer) کاملاً جدا شده است.

*   **Service Layer**: تمام عملیات‌های هسته (Core Operations) در سرویس‌های ایزوله انجام می‌شود:
    *   `UserService`: مدیریت وضعیت کلاینت‌ها، نگاشت Socket ID به موجودیت کاربر و مدیریت نقش‌ها.
    *   `RoomService`: مدیریت چرخه حیات اتاق‌ها، محاسبات آماری لحظه‌ای و ایزولاسیون داده‌ها.
    *   `PermissionService`: لایه امنیتی برای اعتبارسنجی عملیات‌های حساس مدیریتی (RBAC ساده شده).
    *   `MessageService`: استانداردسازی پروتکل تبادل پیام و غنی‌سازی داده‌های خروجی.
*   **Transport Layer**: لایه Socket.io تنها وظیفه جابجایی داده‌ها و فراخوانی سرویس‌های مربوطه را بر عهده دارد.

### ۲. لایه رابط کاربری (Frontend Layer)
*   **Astro Ecosystem**: بهره‌گیری از جزایر تعاملی و مدیریت بهینه اسکریپت‌های سمت کلاینت.
*   **Mobile-First Approach**: طراحی سیستم با اولویت دستگاه‌های موبایل و انتقال نرم به دسکتاپ.

---

## 💎 ویژگی‌های برجسته UI/UX

*   **طراحی Glassmorphism 2.0**: 
    *   استفاده از `backdrop-filter: blur(20px)` عمیق.
    *   گرادینت‌های داینامیک و متحرک در پس‌زمینه.
    *   مرزهای (Borders) نیمه‌شفاف برای ایجاد حس عمق.
*   **تجربه کاربری لابی (Lobby Experience)**:
    *   کارت‌های تعاملی اتاق‌ها با نمایش لحظه‌ای تعداد کاربران.
    *   سیستم ورود سریع (Quick Join) و ساخت اتاق با اعتبارسنجی آنی.
*   **سیستم پیام‌رسانی مدرن**:
    *   بابل‌های چت با طراحی متمایز برای فرستنده و گیرنده.
    *   تایپوگرافی بهینه شده با فونت **Plus Jakarta Sans**.
    *   انیمیشن‌های ورود و خروج نرم (CSS Keyframes).
*   **ریسپانسیو پیشرفته**:
    *   سایدبار کاربران به صورت Drawer در موبایل.
    *   طراحی تطبیق‌پذیر (Adaptive Layout) برای رزولوشن‌های مختلف.

---

## 🛠 جزئیات فنی و پروتکل‌ها

### رویدادهای کلیدی سوکت (Socket Events)

| رویداد | عملکرد | ساختار داده (Payload) |
| :--- | :--- | :--- |
| `roomsList` | دریافت لیست اتاق‌های فعال | `Array<{name: string, count: number}>` |
| `join` | پیوستن به اتاق/لابی | `{username: string, room: string}` |
| `sendMessage` | ارسال پیام متنی | `string` |
| `typing` | وضعیت در حال تایپ | `boolean` |
| `admin:*` | دستورات مدیریتی | `{targetId: string}` |

---

## 📂 ساختار مهندسی پروژه

```text
.
├── __tests__/              # تست‌های واحد و یکپارچه‌سازی (Jest/Supertest)
├── public/
│   └── css/
│       └── style.css       # استایل‌های پیشرفته و متغیرهای UI
├── src/
│   ├── components/
│   │   └── Chat.astro      # منطق کلاینت و رابط کاربری اصلی
│   ├── layouts/
│   │   └── Layout.astro    # ساختار پایه و فونت‌های سیستمی
│   └── server/
│       ├── socket.js       # Orchestrator لایه شبکه
│       └── services/       # Core Business Logic (SOP)
└── README.md
```

---

## 🚀 راه اندازی سریع (Quick Start)

۱. نصب وابستگی‌ها:
```bash
npm install
```

۲. اجرای همزمان سرور و فرانت (در دو ترمینال مجزا):
```bash
# Terminal 1: Backend
npm run server:dev

# Terminal 2: Frontend
npm run dev
```

۳. مشاهده در مرورگر: `http://localhost:3000`

---

## 🧪 تضمین کیفیت (QA)
پروژه دارای پوشش تست مناسب برای سرویس‌های حیاتی است. برای اطمینان از صحت عملکرد منطق برنامه، دستور زیر را اجرا کنید:
```bash
npm test
```

---
**توسعه یافته با ❤️ برای دنیای متن‌باز**
